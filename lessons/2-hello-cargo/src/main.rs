// That first example of "Hello, World!" showed us the basic layout of a Rust
// program, but the process of building and running our binary was a little ...
// archaic. To save us from compilers, dependencies, linkers, and headaches we
// have "cargo"!

fn main() {
    println!("Hello, world!");
}
