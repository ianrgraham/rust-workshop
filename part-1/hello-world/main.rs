// All rust binaries use the "main" function as an entry point.
fn main() {
    
    // Printing to the terminal uses a simple "macro", here "println!".
    // Macros pop up a lot in Rust, but they're quite easy to understand!
    // They dramatically simplify code where a function just won't do it.
    println!("Hello, world!");
}